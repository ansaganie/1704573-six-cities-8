enum SlicesNamespace {
  App = 'app',
  Offer = 'offer',
  Review = 'review',
}

enum AuthStatus {
  Auth = 'auth',
  NoAuth = 'no-auth',
  Unknown = 'unknown'
}

export {
  SlicesNamespace,
  AuthStatus
};
